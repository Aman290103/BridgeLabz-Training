using System;

class AreaOfCircle
{
    static void Main(string[] args)
    {
        double radius = 7;

        double area = Math.PI * radius * radius;
        Console.WriteLine("Area of Circle = " + area);
    }
}
